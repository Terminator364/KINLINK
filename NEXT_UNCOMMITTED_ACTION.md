# NEXT_UNCOMMITTED_ACTION

Resume with `KINLINKGO`.

## P0 communication first

1. verify `.project-memory/RESUME_CAPSULE.json`;
2. read ACTIVE_TRANCHE + communication ledger fresh;
3. if prior tranche is not CLOSED, reconcile Gmail END before any new work;
4. load A+B+C, B5 B31-B100, 80-macro scorecard, dead-end registry and W0-W6 plan;
5. send a fresh Gmail START before substantive continuation.

Communication protocol:
- 22 min work + 3 min close reserve;
- CLOSE_INTENT without END ACK = immediate P0 fault;
- watchdog stale WORKING threshold = 28 min;
- one watchdog only; it never opens an orphan START.

## Canonical scope

- A anchor `c565b6f1164d327caaa17e1de47d1cf48ed3c885`
- B5: **B31-B100**
- C: append-only
- 80 macro capabilities
- **45.1%** conservative maturity
- W2 active
- no micro-beta/user reinstall chain

## W2 evidence already proven

- `f2a6393...`: plan/reserve durable store CI PASS.
- `6f043ab...`: usage reconciliation + Mobile Vault UI machine CI PASS.
- usage semantics:
  - device-wide aggregate ≠ one plan;
  - conflicting exact sources => HOLD;
  - reset/reboot never creates free quota;
  - local plan store schema v2.
- manual 45-shot counter-audit of `6f043ab...` found one blocker:
  **XL-font Mobile Vault dialog clipped the EFFACER action below the viewport.**
- no K017/K068 score advancement is allowed from that head.

## Active exact head

`7d589ddd6038adcb7c318f2b00dc1cf03faaa355`

Fix:
- shorter Mobile Vault labels/message;
- clear action moved inside scrollable form;
- AlertDialog keeps only Enregistrer/Annuler.

Runs:
- design-lint `35664821268`: **PASS**
- Android `35664821258`: **IN PROGRESS at close intent**

## Exact next action

1. poll Android run `35664821258`;
2. if FAIL, read exact logs and fix only evidence-backed cause;
3. if PASS, fetch responsive proof and manually inspect all 45 screenshots, with special focus on all three Mobile Vault dialogs and XL font;
4. only after visual PASS consider:
   - K017 PARTIAL -> IMPLEMENTED_UNQUALIFIED;
   - K068 DESIGN_ONLY -> IMPLEMENTED_UNQUALIFIED;
   - recompute score (expected about 46.0% only if evidence supports both);
5. continue W2 toward optional NetworkStats/per-SIM-safe evidence and full Mobile Vault product surface;
6. no phone install yet.

Gmail START ACK before work; Gmail END ACK + durable CLOSED before app closeout.
