{
  "schema": "kinlink.communication_watchdog_policy/1",
  "project": "KINLINK",
  "name": "KINLINK Comms Watchdog",
  "cardinality": 1,
  "timing_mode": "condition_watch",
  "cadence": "HOURLY",
  "purpose": "repair missed START/END closure and continuity gaps without creating duplicate communication",
  "source_of_truth": [
    ".project-memory/ACTIVE_TRANCHE.json",
    ".project-memory/COMMUNICATION_DELIVERY_LEDGER.jsonl",
    ".project-memory/COMMUNICATION_PROTOCOL.json",
    ".project-memory/COMMUNICATION_STATE_MACHINE.json",
    "project_state.json",
    "NEXT_UNCOMMITTED_ACTION.md",
    "Gmail thread/provider message IDs"
  ],
  "reconciliation_order": [
    "READ_DURABLE_TRANCHE_STATE",
    "READ_LEDGER",
    "VERIFY_GMAIL_THREAD_BEFORE_SEND",
    "IF_END_ALREADY_EXISTS_PERSIST_ACK_ONLY",
    "IF_CHECKPOINTED_OR_CLOSE_INTENT_AND_END_MISSING_SEND_EXACTLY_ONE_END",
    "VERIFY_PROVIDER_ACK",
    "PERSIST_END_ACK_AND_CLOSED",
    "IF_MISSION_CONTINUES_REQUIRE_NEW_TRANCHE_AND_NEW_START_BEFORE_SUBSTANTIVE_WORK"
  ],
  "invariants": [
    "Intermediate PDF/report/artifact mail is neither START nor END unless explicitly typed.",
    "Never reuse a CLOSED tranche.",
    "Never send duplicate END after provider ACK already exists.",
    "Never bypass platform/security controls.",
    "Healthy state produces no notification.",
    "Notify only after a real repair or unresolved fault requiring human action.",
    "Watchdog repairs communication state; it does not silently invent product work progress.",
    "After watchdog closure, no fresh START is opened automatically because the watchdog performs no product work."
  ],
  "idempotency": "Provider message_id/thread_id plus delivery_key are reconciliation keys.",
  "limitation": "Hourly watchdog cannot guarantee sub-hour repair; primary in-turn protocol remains the first line of defense.",
  "stale_work_threshold_minutes": 28,
  "stale_work_recovery": {
    "condition": "START_ACKNOWLEDGED/WORKING with no END and START age > 35 minutes",
    "action": "persist interruption close intent from durable facts only, send/verify exactly one END, persist CLOSED",
    "invent_unproved_work": false
  },
  "foreground_resume": {
    "auto_open_new_start": false,
    "rule": "Watchdog never opens an orphan START. It leaves NEXT_UNCOMMITTED_ACTION intact; the next foreground worker must open a fresh START before substantive work."
  },
  "close_intent_priority": {
    "immediate": true,
    "rule": "CLOSE_INTENT_PERSISTED/END_SEND_PENDING/no_more_product_mutations/status=CLOSING with no durable END ACK is a P0 missed-END fault independent of age.",
    "repair": "verify START thread and existing END first; then persist ACK or send exactly one truthful END; provider readback; persist CLOSED"
  },
  "primary_protocol_deadline": {
    "work_minutes": 22,
    "close_reserve_minutes": 3,
    "expected_end_by_minutes": 25,
    "breach_after_minutes": 28,
    "note": "Automation is hourly and cannot guarantee minute-25 delivery; the foreground protocol remains primary."
  }
}
